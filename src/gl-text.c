#include "gl.h"
#include "colors.h"
#include <stdio.h>
#include <stdlib.h>

// Does this need to be globalized???
TextObject textObjects[MAX_TEXT_OBJECTS];

// inits the text system and shaders, if any step fails, this will return 0, else (succeded) it will return 1
int initTextSystem(void) {
    return 0; // UNDER CONSTRUCTION
}